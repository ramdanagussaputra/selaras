services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: selaras
      POSTGRES_PASSWORD: selaras
      POSTGRES_DB: selaras
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U selaras -d selaras"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  pgdata:
