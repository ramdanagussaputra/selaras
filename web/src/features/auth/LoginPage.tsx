// Placeholder route — real auth lands in M2.
export function LoginPage() {
  return (
    <section>
      <h1 className="text-2xl font-semibold">Login</h1>
      <p className="mt-2 text-slate-600">Authentication arrives in milestone M2.</p>
    </section>
  );
}
