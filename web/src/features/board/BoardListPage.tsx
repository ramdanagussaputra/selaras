// Placeholder route — board CRUD lands in M3.
export function BoardListPage() {
  return (
    <section>
      <h1 className="text-2xl font-semibold">Your boards</h1>
      <p className="mt-2 text-slate-600">
        Walking skeleton: every layer is wired (React → Go → Postgres) with zero features.
        Boards arrive in milestone M3.
      </p>
    </section>
  );
}
