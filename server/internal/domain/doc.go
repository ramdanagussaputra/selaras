// Package domain is the core: pure business logic and the ports the core
// needs, with zero external imports (enforced by deprule_test.go).
// M1 (foundation) contributes only the health.Pinger port.
package domain
